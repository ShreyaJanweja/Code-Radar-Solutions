// Your code here...
#include<stdio.h>
void prime();

int main(){
    int inputs , n;
    scanf("%d", &inputs); 
    prime();
    return 0;
}

void prime(){
    while(n != inputs){
        scanf("%d", &n);
    } 
    while(n >= 0){
        result = (n % 1 == 0) && (n % n == 0);
        printf(result);
    }
};

